Вариант 3
(7 баллов) Напишите ObjectCloner. Метод Clone(object obj) 
создает новый экземпляр того же типа (Activator.CreateInstance) 
и копирует туда значения всех публичных свойств (считаем, что ссылочных полей нет, только int, string, bool).

(7 баллов) Напишите TaskQueueManager: создайте Queue<Action> и два потока. 
Первый поток (Producer) закидывает в очередь 20 действий, второй (Consumer) забирает их из очереди и выполняет 
(Для понимания когда остановиться, сделайте у каждого из потоков свой счетчик и перед запуском передавайте количество объектов,
 которые он должен обработать). Очередь должна быть потокобезопасной.

(1 балл) Объедините: перед тем как Producer закинет Action в очередь, он должен создать клон объекта (из задачи №1)
 и передать этот клон внутрь Action для работы (например, вывода в консоль типа).